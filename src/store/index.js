import { createStore } from 'vuex'

// Modules
import userData from './modules/user-data'

export default createStore({
  state: {
    showCriar: false
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userData
  }
})
